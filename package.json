{
  "private": true,
  "name": "xenialauncher",
  "version": "0.0.0",
  "license": "GPL v3",
  "scripts": {
    "move_files": "cp -r ./resources/ ./build/resources",
    "dev": "preact build --no-prerender && yarn move_files && neu run",
    "build": "preact build --no-prerender && yarn move_files && neu build"
  },
  "dependencies": {
    "preact": "^10.3.2",
    "preact-render-to-string": "^5.1.4",
    "preact-router": "^3.2.1"
  }
}